import { AnyClass, CommandResolvers, Workerized } from "../interfaces";
/**
 * Create a proxy to the worker contained object
 * @internal
 * @param workerizedClass Proxied class
 * @param worker Worker containing the class instance
 * @param resolvers Map holding all commands resolvers
 * @returns Proxied object
 */
export declare function createWorkerized<W extends Object>(workerizedClass: AnyClass, worker: Worker, resolvers: CommandResolvers): Promise<Workerized<W>>;
