/**
 * Test if a argument must be transferred to the worker
 * @internal
 * @param arg Argument to test
 * @returns Must argument being transferred?
 */
export function isTransferable(arg: any): boolean {
  const transferableTypes = [ImageBitmap, ReadableStream, WritableStream];
  return transferableTypes.some((type) => arg instanceof type);
}
