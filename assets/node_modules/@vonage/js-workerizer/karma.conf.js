module.exports = function (config) {
  config.set({
    basePath: "",
    frameworks: ["karma-vite3", "mocha", "chai"],
    files: [
      {
        pattern: "tests/**/*.ts",
        type: "module",
        watched: false,
        served: false,
      },
    ],
    exclude: [],
    preprocessors: {},
    reporters: ["progress"],
    port: 9876,
    colors: true,
    logLevel: config.LOG_INFO,
    browsers: ["ChromeHeadless"],
    singleRun: false,
    concurrency: Infinity,
    customLaunchers: {
      ChromeHeadless_with_debugging: {
        base: "ChromeHeadless",
        flags: ["--remote-debugging-port=9222"],
        debug: true,
      },
    },
  });
};
