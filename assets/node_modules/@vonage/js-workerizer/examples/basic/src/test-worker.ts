import { registerWorker } from "../../../src/index";

export class TestWorker {
  public run() {
    const start = Date.now();
    while (Date.now() - start < 2000) {}
  }
  static {
    registerWorker("TestWorker", this);
  }
}
