import { TextureSource } from "../../types";
import { WebglPipeline } from "../webgl-pipeline";
export declare class ResizeImage extends WebglPipeline {
    private readonly context;
    inputImage?: ImageBitmap | TextureSource | WebGLTexture;
    constructor(context: WebGLRenderingContext, width: number, height: number);
    setData(image?: ImageBitmap | TextureSource | WebGLTexture): void;
}
