import { TextureSource } from "../../types";
import { WebglPipeline } from "../webgl-pipeline";
/**
 * @internal
 */
export interface FlickeringOptions {
    currentThresholdMinimum: number;
    currentThresholdMaximum: number;
    previousThresholdMinimum: number;
    previousThresholdMaximum: number;
    thresholdMinimum: number;
    thresholdMaximum: number;
    thresholdFactor: number;
}
export declare class ImproveSegmentationMask extends WebglPipeline {
    private readonly context;
    inputImage?: ImageBitmap;
    inputMask?: ImageBitmap;
    previousInputImage?: ImageBitmap;
    previousInputMask?: ImageBitmap;
    output: TextureSource | WebGLTexture;
    private options;
    constructor(context: WebGLRenderingContext, width: number, height: number);
    setData(image?: ImageBitmap, mask?: ImageBitmap): void;
    setOptions(options: FlickeringOptions): void;
}
