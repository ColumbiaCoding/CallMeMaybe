import { TextureSource } from "../types";
import { WebglPipeline } from "./webgl-pipeline";
export declare abstract class WebglTransformerPipeline extends WebglPipeline {
    preciseMaskEnabled: boolean;
    abstract setData(image?: ImageBitmap | TextureSource | WebGLTexture, mask?: ImageBitmap | TextureSource | WebGLTexture): void;
}
