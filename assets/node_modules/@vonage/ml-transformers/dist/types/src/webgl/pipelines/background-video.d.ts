import { BackgroundImage } from "./background-image";
export declare class BackgroundVideo extends BackgroundImage {
    private frameReader?;
    preciseMaskEnabled: boolean;
    set inputVideo(videoBGReadable: ReadableStream);
    run(): Promise<void>;
    private readNextVideoFrame;
}
