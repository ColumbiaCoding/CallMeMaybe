import { WebglPipelineProgram, WebglPipelineProgramOptions } from "../../webgl-pipeline-program";
export declare enum BLUR_TYPE {
    LINEAR = 0,
    GAUSSIAN = 1
}
export interface BlurOptions extends WebglPipelineProgramOptions {
    radius?: number;
    type?: BLUR_TYPE;
}
export declare class Blur extends WebglPipelineProgram<BlurOptions> {
    protected getDefines(): {
        [key: string]: number;
    };
    protected getFragmentShader(): string;
    protected getVertexShader(): string;
    private createLoopFunction;
    private getFactor;
    private getLinearFactor;
    private getGaussianFactor;
}
