import * as twgl from "twgl.js";
import { UniformDataMap } from "../types";
import { WebglPipelineProgram, WebglPipelineProgramOptions } from "./webgl-pipeline-program";
/**
 * Webgl pipeline program where two framebuffer are used.
 * The framebuffer will alternate between two run.
 * Previous framebuffer texture will be binded to previous_rendering sampler2D uniform
 *
 */
export declare abstract class WebglPipelineProgramDoubleBuffer extends WebglPipelineProgram {
    fbi2: twgl.FramebufferInfo;
    constructor(options: WebglPipelineProgramOptions);
    run(uniforms: UniformDataMap): void;
    resizeOutput(width: number, height: number): void;
}
